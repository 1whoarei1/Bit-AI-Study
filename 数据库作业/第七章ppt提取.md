第七章  数据库的完整性
第7章  数据库完整性
数据库的完整性
数据的正确性、有效性和相容性
防止数据库中存在不符合语义的数据，也就是防止数据库中存在不正确的数据
防范对象：不合语义的、不正确的数据

数据库的安全性
保护数据库防止恶意的破坏和非法的存取
防范对象：非法用户和非法操作
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.1 数据库的完整性概述
数据库的完整性是一种语义概念，
防止数据库中存在
不符合语义的数据，
不正确的数据
保证数据库中数据的质量。 
为维护数据库的完整性，DBMS必须：
提供定义完整性约束条件的机制
提供完整性检查的方法
违约处理
7.1 数据库的完整性概述
完整性约束条件
加在数据上的语义约束条件
它一般是对数据库中数据本身的某种语义限制、数据间的逻辑约束和数据变化时所遵循的规则等
表达了给定的数据模型中数据及其联系所具有的制约和依存规则
用以限定符合数据模型的数据库状态及状态的变化
7.1 数据库的完整性概述
（1） 列级约束
主要是对属性的数据类型、数据格式和取值范围、精度等的约束。具体包括：
对数据类型的约束，包括数据类型、长度、精度等的约束。例如学生姓名的数据类型是字符型，长度是8。
对数据格式的约束，例如规定日期的格式为YYYY/MM/DD
对取值域的约束，例如学生成绩的取值范围必须是0～100。
对空值的约束，有些字段允许为空，有些不行 
7.1 数据库的完整性概述
（2） 元组约束
一个元组是由若干个属性组成的，元组级约束就是元组中各个属性之间的约束关系。例如订货关系中发货日期不能小于订货日期，发货量不得超过订货量等。
（3） 关系约束
关系约束是指一个关系的各个元组之间、或者多个关系之间存在的各种联系或约束。常见的关系约束有实体完整性约束、参照完整性约束、函数依赖约束、统计约束等。例如规定某个部门的部门经理的工资不能高于本部门普通职工平均工资的5倍。
7.1 数据库的完整性概述
（1）静态约束
某个确定的状态时数据的约束条件。例如数据的取值范围、数据类型。
（2）动态约束
从一个状态到另一个状态时新旧值之间的约束条件。例如对年龄的修改只能增加不能减小。
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.2 实体完整性
实体完整性规则规定：
主键的值不能取空值
主键的值唯一
实现方法：通过对主键值的约束实现实体完整性。 
7.2.1 实体完整性的定义
关系模型的实体完整性
CREATE  TABLE中用PRIMARY KEY定义
单属性构成的主键有两种说明方法 
定义为列级约束条件、定义为表级约束条件
对多个属性构成的主键只有一种说明方法
定义为表级约束条件 
7.2.1 实体完整性的定义
【例】将Student表中的Sno属性定义为主键
7.2.1 实体完整性的定义
为了实施实体完整性，系统将在主键属性上自动创建唯一的索引来强制唯一性约束。 
7.2.2	实体完整性检查和违约处理
定义表的主键后，每当对该表插入一条记录或者对主键进行更新操作时，DBMS自动进行实体完整性的检查
检查主键是否唯一
如果不唯一则拒绝进行插入或修改；
检查主键的各个属性（字段）值是否为空
如果有空的字段值，则拒绝操作；
从而保证实体完整性。
7.2.2	实体完整性检查和违约处理
使用全表扫描法检查记录中主键值是否唯一
7.2.2	实体完整性检查和违约处理
使用索引判断主键是否存在
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.3  参照完整性
参照完整性规则规定：
外键的值要么为空
要么只能取被参照的对象的值
实现方法：通过对外键值的约束实现参照完整性

7.3.1 参照完整性的定义
关系模型的参照完整性
用FOREIGN KEY短语定义哪些列为外键,
用REFERENCES短语指明外键参照哪些表的主键
7.3.1 参照完整性的定义
【例】定义SC中的参照完整性
     CREATE TABLE SC
         ( Sno    CHAR(9)  NOT NULL， 
          Cno     CHAR(4)  NOT NULL，  
          Grade    SMALLINT，
          PRIMARY KEY (Sno， Cno)，  
          FOREIGN KEY (Sno) REFERENCES Student(Sno)，  
          FOREIGN KEY (Cno) REFERENCES Course(Cno)    
          );

外键约束的例子（被参照表参照表）
分析外键约束（被参照表参照表）
对被参照表中的主键操作
插入：要求插入值满足主键限制即可，不影响其它表
修改：可能会影响与该主键相关的参照表的外键值。当相应的外键值存在时，策略有三：一是改变对应参照表的所有外键值，使之与主键一致；二是不允许修改被参照表中的主键值；三是对相应的外键值置空。
删除：可能会影响与该主键相关的参照表的外键值。若相应的外键值存在时，策略有三：一是不允许删除被参照表的主键值；二是级联删除参照表中相应外键值所在的行；三是对相应的外键值置空。
。

外键约束的例子（参照表被参照表）
分析外键约束（参照表被参照表）
对参照表中的外键操作
插入：要求插入的外键值应“参照”（Reference）被参照表中的主键值，或者置空。 
修改：要求修改的外键值“参照”被参照表中的主键值 ，或者置空。 
删除：不需要参照被参照表中的主键值，不影响其他表。

可能破坏参照完整性的情况及违约处理
7.3.2	参照完整性检查和违约处理
数据库管理系统提供不同的违约处理策略供用户选择
参照完整性违约处理
拒绝(NO ACTION)执行
默认策略
级联(CASCADE)操作
设置为空值（SET NULL）
对于参照完整性，除了应该定义外键，还应定义外键列是否允许空值
当对参照表或被参照表的操作违反了参照完整性时，系统选择默认策略，即拒绝执行。如果想让系统采用其他策略，则必须要显示的加以说明。



7.3.2	参照完整性检查和违约处理
【例】显示定义参照完整性的违约处理方式
CREATE TABLE SC
    (Sno CHAR(9) NOT NULL,
     Cno CHAR(4) NOT NULL,
     GRADE SMALLINT,
     PRIMARY KEY(Sno, Cno),
     FOREIGN KEY(Sno) REFERENCES Student(Sno)
        ON DELETE CASCADE
        /*当删除Student表中元组时，级联删除SC表中相应的元组*/
        ON UPDATE CASCADE,
     FOREIGN KEY(Cno) REFERENCES Course(Cno)
        ON DELETE NO ACTION
    /*当删除Course表中元组时，如果SC表中存在外键值与之对应则拒绝删除*/
        ON UPDATE CASCADE);
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.4 用户定义的完整性
用户定义的完整性就是针对某一具体应用的数据必须满足的语义要求 
RDBMS提供，而不必由应用程序承担
属性上的约束条件
唯一(UNIQUE)、非空(NOT NULL)、CHECK约束、默认值DEFAULT等 
元组上的约束条件 
用CHECK短语定义元组上的约束条件，即元组级的限制，设置不同属性之间的相互约束条件 
插入元组或修改属性的值时，RDBMS检查约束条件是否被满足，如果不满足则拒绝执行 
7.4 用户定义的完整性
【例】创建table1，指定c1字段不能包含重复值，c2字段只能取特定值，c3字段默认值为1。

        CREATE TABLE table1
           (   c1  CHAR(2) UNIQUE，
               c2  CHAR(4) CHECK (c2 IN ('0000', '0001', '0002', '0003')), 
               c3  INT  DEFAULT 1
            ) ;

7.4 用户定义的完整性
【例】当学生的性别是男时，其名字不能以Ms.打头
    CREATE TABLE Student
         (Sno    CHAR(9)， 
          Sname  CHAR(8) NOT NULL，
          Ssex    CHAR(2) CHECK(Ssex IN(‘男’,‘女’))，
          Sage   SMALLINT，
          Sdept  CHAR(20)，        
          PRIMARY KEY (Sno)，
          CHECK (Ssex='女' OR Sname NOT LIKE 'Ms.%')
    /*定义了元组中Sname和 Ssex两个属性值之间的约束条件*/
        )；

性别是女性的元组都能通过检查，因Ssex=‘女’成立
当性别是男性时，要通过检查即：名字一定不能以Ms.打头
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.5 完整性约束命名子句
SQL提供了完整性约束命名子句CONSTRAINT，用来对完整性约束条件命名，从而可以灵活的增加或者删除一个完整性约束条件。
CONSTRAINT <完整性约束条件名><完整性约束条件>
<完整性约束条件>包括：
PRIMARY KEY 
FOREIGN KEY
NOT NULL
UNIQUE
CHECK短语
……

7.5 完整性约束命名子句
【例】建立学生登记表Student，要求学号在90000~99999之间，姓名不能取空值，年龄小于30，性别只能是“男”或“女”。
    CREATE TABLE Student
      (Sno  NUMERIC(5)
        CONSTRAINT C1 CHECK (Sno BETWEEN 90000 AND 99999)，
        Sname  CHAR(20)  
        CONSTRAINT C2 NOT NULL，
        Sage  NUMERIC(3)
        CONSTRAINT C3 CHECK (Sage < 30)，
        Ssex  CHAR(2)
        CONSTRAINT C4 CHECK (Ssex IN ( '男'，'女'))，
        CONSTRAINT StudentKey PRIMARY KEY(Sno)
      )；
在Student表上建立了5个约束条件，包括主键约束（命名为StudentKey）以及C1、C2、C3、C4四个列级约束。
7.5 完整性约束命名子句
修改表中的完整性限制
使用ALTER TABLE语句修改表中的完整性限制

【例】修改表Student中的约束条件，要求学号改为在99000~99999之间，年龄由小于30改为小于40
可以先删除原来的约束条件，再增加新的约束条件
      ALTER TABLE Student
             DROP CONSTRAINT C1;
        ALTER TABLE Student
             ADD CONSTRAINT C1 CHECK (Sno BETWEEN 99000 AND 99999);
        ALTER TABLE Student
             DROP CONSTRAINT C3;
        ALTER TABLE Student
            ADD CONSTRAINT C3 CHECK (Sage < 40);
第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.6  触发器
数据库系统一般提供两种主要机制来实现业务规则和数据完整性
约束
DBMS提供定义完整性约束条件的机制，在检测出违反约束条件的操作后,进行相应的违约处理。但是，只能作简单的动作，例如拒绝操作。
触发器
触发器是用户定义在关系数据表上的一类由事件驱动的特殊过程，用编程的方法实现复杂的业务规则
触发器比约束更加灵活，可以实现一般的数据完整性约束实现不了的复杂的完整性约束，具有更精细和更强大的数据控制能力
7.6.1	触发器概述
触发器在对表或视图发出 UPDATE、INSERT 或 DELETE 语句时自动执行 
可用触发器完成很多数据库完整性保护的功能
触发器的优点： 
实现复杂的业务规则
实现比CHECK 约束更复杂的数据完整性
比较数据修改前后的状态
维护非规范化数据
7.6.2	定义触发器
SQL使用CREATE TRIGGER命令创建触发器:
CREATE TRIGGER <触发器名>  
     { BEFORE | AFTER} <触发事件> ON <表名>
        FOR EACH  {ROW | STATEMENT}
       ［WHEN <触发条件>］
          <触发动作体>
定义触发器的语法说明:
1. 创建者：表的拥有者
2. 表名：触发器的目标表
3. 触发事件：INSERT、DELETE、UPDATE，或者是这几个事件的组合，如INSERT OR DELETE等，还可以是UPDATE OF<列,…>等
7.6.2	定义触发器
4. 触发时间 
BEFORE
表示在触发事件进行以前，判断触发条件是否满足。
若满足条件则先执行触发动作体的操作，
然后再执行触发事件的操作。 
AFTER
表示在触发事件完成之后，判断触发条件是否满足。
若满足条件则执行触发动作体的操作
如果触发事件因错误（如违反约束或语法错误）而失败，触发器将不会执行
7.6.2	定义触发器
5. 触发器类型
行级触发器(FOR EACH ROW)。
对每一个修改的元组都会触发触发器的检查和执行 
语句级触发器(FOR EACH STATEMENT)。
只在SQL语句执行时候进行触发条件的检查和触发器的执行，只触发1次 
   【例】Teacher表上创建一个AFTER UPDATE触发器。如果表Teacher有1000行，执行如下语句：
              UPDATE Teacher SET Deptno=5; 
行级触发器：触发动作将执行1000次
语句级触发器：触发动作只发生一次 
7.6.2	定义触发器
6. 触发条件
触发条件为真：触发动作体才执行
省略：触发动作体总是执行
7. 触发动作体
触发动作体是满足触发条件后，执行的一系列数据库操作。
如果触发动作体执行失败，激活触发器的事件就会终止执行，触发器的目标表或触发器可能影响的其他对象不发生任何变化 
7.6.2	定义触发器
定义一个BEFORE行级触发器，为教师表Teacher定义完整性规则“教授的工资不得低于4000元，如果低于4000元，自动改为4000元”。

7.6.2	定义触发器
定义AFTER行级触发器，当教师表Teacher的工资发生变化后就自动在工资变化表Sal_log中增加一条相应记录

7.6.2	定义触发器
定义AFTER行级触发器，当教师表Teacher的工资发生变化后就自动在工资变化表Sal_log中增加一条相应记录

7.6.2	定义触发器
定义AFTER行级触发器，当教师表Teacher的工资发生变化后就自动在工资变化表Sal_log中增加一条相应记录

7.6.3	使用触发器
触发器的执行，是由触发事件激活的，并由数据库服务器自动执行
一个数据表上可能定义了多个触发器
同一个表上的多个触发器激活时遵循的执行顺序：
（1） 执行该表上的BEFORE触发器；
（2） 激活触发器的SQL语句；
（3） 执行该表上的AFTER触发器。
7.6.3	使用触发器
执行修改某个教师工资的SQL语句，激活上述定义的触发器。
UPDATE Teacher SET Sal=800 WHERE Ename='陈平';
执行顺序是：
执行触发器Insert_Or_Update_Sal
假设陈平是教授，那么:new.Sal :=4000
执行SQL语句“UPDATE Teacher SET Sal=800 WHERE Ename='陈平';”
执行触发器Update_Sal
假设陈平之前的工资不是4000，那么在工资变化表上插入一条新记录 

7.6.4	删除触发器
删除触发器的SQL语法
    DROP TRIGGER <触发器名> ON <表名>;
触发器必须是一个已经创建的触发器，并且只能由具有相应权限的用户删除。

【例】删除教师表Teacher上的触发器Insert_Sal
     DROP TRIGGER Insert_Sal ON Teacher;


第7章  数据库的完整性
7.1	数据库的完整性概述
7.2	实体完整性
7.3	参照完整性
7.4	用户定义的完整性
7.5	完整性约束命名子句
7.6	 触发器
7.7  小结
7.7 小结
应综合考虑功能性和性能开销， 来决定使用约束还是触发器来保证数据库的完整性
对于基本的完整性逻辑，例如有效值和维护表间的关系，最好使用声明式完整性约束
如果要维护复杂的、大量的、非主键或外键关系一部分的数据，则选择使用触发器

7.7 小结
数据库的完整性是为了保证数据库中存储的数据是正确的、相容的。
RDBMS完整性实现的机制
完整性约束定义机制
完整性检查机制
违背完整性约束条件时RDBMS应采取的动作
实体完整性
参照完整性
用户定义的完整性
实现数据库完整性的一个重要方法——触发器


1. DBMS的完整性控制机制应具有哪三方面的功能？ 
2. 请列举可能违反参照完整性的各种情况，以及相应的违约处理策略。
3.	考虑如下关系数据库：
学生表Student(Sno, Sname, Sage, Ssex)
系表Department(Dno, Dname)
学习表Study(Sno, Dno, rank)
    请使用触发器实现要求：在对Student表插入新学生时，也将此学生信息插入到Study表中，不确定的属性置空。

4.	在如下关系数据库中完成以下操作：
books (编号，书名，出版社，定价) 
readers (编号，姓名，读者类型，已借数量)
borrowinf (图书编号，读者编号，借期，还期)
readertype (类型编号，类型名称)
    有以下约束条件：
    （1）borrowinf中“还期”不能小于“借期” 。
    （2）readers的列“读者类型”上的默认值为3。
    （3）readers的“已借数量”的限定条件为值不小于零。
      请根据实际情况设计各个表上的实体完整性、参照完整性以及用户自定义的完整性，并用SQL语句完成上述关系表的定义以及实现这些约束条件。
